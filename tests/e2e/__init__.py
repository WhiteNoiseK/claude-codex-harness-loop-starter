"""E2E-test package — full-pipeline integration scenarios (slow).

A marker file that lets pytest recognize the directory as a package. No import side effects.
Since runtime can be long, combining it with @pytest.mark.slow is recommended.
"""
