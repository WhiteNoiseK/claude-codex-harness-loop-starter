"""Unit-test package — verifies individual modules/functions (no external I/O, mocks only).

A marker file that lets pytest recognize the directory as a package. No import side effects.
Targeted by the @pytest.mark.unit marker. Verifies pure logic with no disk/network/hardware access.
"""
